spring:
  config:
    activate:
      on-profile: prod
  application:
    name: UserSociableSphere
  r2dbc:
    url: r2dbc:postgresql://socialsphere_root:socialsphere@postgresql-socialsphere.alwaysdata.net:5432/socialsphere_users
    username: socialsphere_root
    password: socialsphere
  logging:
    level:
      root: DEBUG
  data:
    r2dbc:
      repositories:
        enabled: true
  sql:
    init:
      mode: never

server:
  port: 8081